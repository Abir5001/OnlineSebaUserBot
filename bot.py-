from telegram import Update, InlineKeyboardButton, InlineKeyboardMarkup
from telegram.ext import (
    Application,
    CommandHandler,
    CallbackQueryHandler,
    ContextTypes,
)

# ==============================
# এখানে আপনার Bot Token বসাবেন
# ==============================
BOT_TOKEN =8527971654:AAF18ViwuhBoFINPxWSU2Ln4QwbUYMtAgUM

# ==============================
# এখানে আপনার Telegram Username দিন
# 
# ==============================
ADMIN_USERNAME = Yasin1335764


SERVICES = {
    "facebook": (
        "👥 Facebook Follower Service\n\n"
        "Facebook পেজ/প্রোফাইলের ফলোয়ার সংক্রান্ত সার্ভিস।\n\n"
        "📦 অর্ডার করতে নিচের Admin Contact বাটনে যোগাযোগ করুন।"
    ),
    "verification": (
        "🔵 Facebook & Instagram Verification\n\n"
        "Facebook ও Instagram verification সংক্রান্ত সহায়তা।\n\n"
        "📦 বিস্তারিত জানতে Admin-এর সাথে যোগাযোগ করুন।"
    ),
    "bybit": (
        "💳 Bybit Card & Digital Service\n\n"
        "Bybit Card ও বিভিন্ন ডিজিটাল সার্ভিস সংক্রান্ত সহায়তা।\n\n"
        "📦 অর্ডার বা তথ্যের জন্য Admin-এর সাথে যোগাযোগ করুন।"
    ),
    "ads": (
        "📢 Facebook & Instagram Ads\n\n"
        "Facebook ও Instagram বিজ্ঞাপন/Ads সংক্রান্ত সার্ভিস।\n\n"
        "📦 আপনার প্রয়োজন অনুযায়ী Ads Service নিতে Admin-এর সাথে যোগাযোগ করুন।"
    ),
    "social": (
        "📱 Social Media Service\n\n"
        "বিভিন্ন Social Media সংক্রান্ত সার্ভিস।\n\n"
        "📦 বিস্তারিত জানতে Admin-এর সাথে যোগাযোগ করুন।"
    ),
    "youtube": (
        "▶️ YouTube Service\n\n"
        "YouTube চ্যানেল ও বিভিন্ন YouTube সংক্রান্ত সার্ভিস।\n\n"
        "📦 অর্ডার করতে Admin-এর সাথে যোগাযোগ করুন।"
    ),
    "website": (
        "🌐 Website & Landing Page\n\n"
        "Website ও Landing Page তৈরি/সেটআপ সংক্রান্ত সার্ভিস।\n\n"
        "📦 আপনার প্রয়োজন জানাতে Admin-এর সাথে যোগাযোগ করুন।"
    ),
    "recovery": (
        "🔐 Account & Page Recovery Help\n\n"
        "Account ও Page recovery সংক্রান্ত বৈধ সহায়তা।\n\n"
        "⚠️ শুধুমাত্র নিজের বা অনুমোদিত অ্যাকাউন্টের জন্য সহায়তা দেওয়া হবে।\n\n"
        "💬 বিস্তারিত জানতে Admin-এর সাথে যোগাযোগ করুন।"
    ),
    "other": (
        "🛠️ অন্যান্য অনলাইন সার্ভিস\n\n"
        "আপনার প্রয়োজনীয় সার্ভিসটি আমাদের জানান।\n\n"
        "💬 Admin-এর সাথে যোগাযোগ করে বিস্তারিত আলোচনা করুন।"
    ),
}


def main_menu():
    keyboard = [
        [
            InlineKeyboardButton("👥 Facebook Follower", callback_data="facebook"),
            InlineKeyboardButton("🔵 Verification", callback_data="verification"),
        ],
        [
            InlineKeyboardButton("💳 Bybit & Digital", callback_data="bybit"),
            InlineKeyboardButton("📢 Facebook & Instagram Ads", callback_data="ads"),
        ],
        [
            InlineKeyboardButton("📱 Social Media", callback_data="social"),
            InlineKeyboardButton("▶️ YouTube Service", callback_data="youtube"),
        ],
        [
            InlineKeyboardButton("🌐 Website & Landing Page", callback_data="website"),
            InlineKeyboardButton("🔐 Account Recovery", callback_data="recovery"),
        ],
        [
            InlineKeyboardButton("🛠️ অন্যান্য সার্ভিস", callback_data="other"),
        ],
        [
            InlineKeyboardButton(
                "💬 Admin-এর সাথে যোগাযোগ",
                url=f"https://t.me/{ADMIN_USERNAME}"
            )
        ],
    ]

    return InlineKeyboardMarkup(keyboard)


async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    text = (
        "🌐 *অনলাইন সার্ভিস সেন্টারে আপনাকে স্বাগতম।*\n\n"
        "ফেসবুক, ইনস্টাগ্রাম, বিজ্ঞাপন, ডিজিটাল মার্কেটিং "
        "ও অন্যান্য অনলাইন সার্ভিস একসাথে।\n\n"
        "📦 আপনার প্রয়োজনীয় সার্ভিস নির্বাচন করুন।\n"
        "💬 যেকোনো সহায়তার জন্য Admin-এর সাথে যোগাযোগ করুন।"
    )

    await update.message.reply_text(
        text,
        parse_mode="Markdown",
        reply_markup=main_menu()
    )


async def service_details(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    await query.answer()

    service_text = SERVICES.get(query.data)

    if not service_text:
        return

    keyboard = [
        [
            InlineKeyboardButton(
                "📦 অর্ডার / বিস্তারিত জানতে",
                url=f"https://t.me/{ADMIN_USERNAME}"
            )
        ],
        [
            InlineKeyboardButton(
                "🔙 মূল মেনু",
                callback_data="home"
            )
        ],
    ]

    await query.edit_message_text(
        service_text,
        reply_markup=InlineKeyboardMarkup(keyboard)
    )


async def home(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    await query.answer()

    text = (
        "🌐 *অনলাইন সার্ভিস সেন্টার*\n\n"
        "আপনার প্রয়োজনীয় সার্ভিস নির্বাচন করুন 👇"
    )

    await query.edit_message_text(
        text,
        parse_mode="Markdown",
        reply_markup=main_menu()
    )


def run_bot():
    app = Application.builder().token(BOT_TOKEN).build()

    app.add_handler(CommandHandler("start", start))

    app.add_handler(
        CallbackQueryHandler(
            home,
            pattern="^home$"
        )
    )

    app.add_handler(
        CallbackQueryHandler(
            service_details,
            pattern="^(facebook|verification|bybit|ads|social|youtube|website|recovery|other)$"
        )
    )

    print("🤖 Online Service Bot is running...")
    app.run_polling()


if __name__ == "__main__":
    run_bot()
